name: word_learner
description: A simple app for learning new words.
version: 1.0.0
environment:
  sdk: '>=2.19.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  file_picker: ^10.3.3
  fl_chart: ^0.70.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  hive_generator: ^2.0.0
  build_runner: ^2.4.13

flutter:
  uses-material-design: true